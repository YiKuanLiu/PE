# Core stack for the PE NCCT classification pipeline.
# Install into a fresh conda env (python 3.10) -- see environment.yml / README.
# PyTorch is installed separately from the CUDA index, e.g.:
#   pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
monai==1.3.0
scikit-learn==1.3.2
scipy==1.11.4
pandas
numpy<2
nibabel
einops
tqdm
tensorboard
h5py
matplotlib
pyyaml
